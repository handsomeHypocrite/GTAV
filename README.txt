1. Install the packages using pip:
python -m pip install --upgrade pip
pip install Pillow
pip install opencv-python
pip install numpy

2. Run all on Jupyter notebook

3. Play some video on top left corner of your screen

4. open the new window in task bar

5. If the above steps don't work try:
	> not to be an idiot